# Core

`WhatIfSimulator/Assets/Core` is the canonical home for simulation engine code and subsystem implementation.

Future work in this folder includes:

- `SimulationEngine`
- Tick orchestration
- Population, economy, technology, and conflict systems
- Deterministic utilities and validation code that directly supports simulation execution

This folder is intentionally reserved during EPIC-00 so later epics build on a stable layout.
